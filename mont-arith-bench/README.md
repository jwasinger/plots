# Go-ethereum EVM384 "and Friends" Benchmarks with Graphs

## Setup/Requirements
Have `gnuplot` installed and in the `$PATH`

## Usage
Generate graphs of benchmarks from the traces of geth opcode benchmarks under `raw_data`:
```
make
```
